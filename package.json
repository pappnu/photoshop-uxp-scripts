{
  "type": "module",
  "scripts": {
    "build": "tsc && node --experimental-transform-types tools/build.ts",
    "type-check": "tsc --noEmit && tsc --project tsconfig.tools.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/photoshop": "^25.0.4",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  }
}
